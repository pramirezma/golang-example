apiVersion: v1
kind: Service
metadata:
    name: "personalcluster-bdea"
    labels:
        app: "personalcluster-bdea"
spec:
    type: ClusterIP
    ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: http
    selector:
        app: "personalcluster-bdea"